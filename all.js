 else {
            entry.target.classList.remove('show');
        }

// to put if you need the text to disappear when not visible // 

let name = 'Ola';

let selectedColor = null;

// we use null to empty the variable , so then we can reassign it to some other value
//it's different from undefined value

let name = 'Ola';
let age = '23';

let person = {
    name: 'Ola',
    age: 23
};

console.log(person);


#myVideo {
    position: fixed;
    right: 0;
    bottom: 0;
    min-width: 100%;
    min-height: 100%;
    z-index: -1;
  }